apiVersion: v1
kind: Service
metadata:
  name: egress-http
  labels:
    app.kubernetes.io/name: minicloud
    app.kubernetes.io/component: egress-http
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egress-http
  ports:
    - name: http
      port: 8080
      targetPort: http
