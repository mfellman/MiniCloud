apiVersion: v1
kind: Service
metadata:
  name: egress-ftp
  labels:
    app.kubernetes.io/name: minicloud
    app.kubernetes.io/component: egress-ftp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: egress-ftp
  ports:
    - name: http
      port: 8080
      targetPort: http
